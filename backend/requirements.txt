fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
pydantic[email]>=2.7,<3.0
pydantic-settings>=2.3,<3.0
PyJWT>=2.8,<3.0
bcrypt>=4.1,<5.0
cryptography>=50.0.1,<51
pyotp>=2.9,<3.0
python-multipart>=0.0.9
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0; platform_system != "Windows"
# Optional: only used when ACP_REDIS_URL is set (cross-worker auth rate limit).
redis>=5,<6
# Guarantees the IANA timezone database for zoneinfo (access schedules and the
# attendance report), independent of the base image's system tz data.
tzdata>=2024.1
